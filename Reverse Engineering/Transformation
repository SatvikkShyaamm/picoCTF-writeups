PROBLEM STATEMENT:
I wonder what this really is...
''.join([chr((ord(flag[i]) << 8) + ord(flag[i + 1])) for i in range(0, len(flag), 2)])

HINTS:
You may find some decoders online

APPROACH:
First download the file given
copy the contents of that file
craft the python code to decode the contents encrypted
Run the python code in the terminal and that will print the flag
